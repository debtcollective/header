---
name: Profile
---

import { Playground, PropsTable } from "docz";
import { ProfileItems } from "./ProfileItems";
import { AlertsDropdown } from "./AlertsDropdown";
import { InboxDropdown } from "./InboxDropdown";
import { UserAvatarDropdown } from "./UserAvatarDropdown";
import { notifications } from "../__fixtures__/notifications";

# Profile

An scope to manage all presentational components around profile displaying, such as:

- **AlertsDropdown**: displays an icon that toggles dropdown with alerts
- **InboxDropdown**: displays an icon that toggles dropdown with messages
- **UserAvatarDropdown**: displays an icon or picture if available that toggles dropdown with profile actions
- **ProfileItems**: serves as a wrapper to hold the composition of all profile components together

This module expose all the components as they are being intended to use as isolated pieces to render data

## AlertsDropdown

<PropsTable of={AlertsDropdown} />

## InboxDropdown

<PropsTable of={InboxDropdown} />

## UserAvatarDropdown

<PropsTable of={UserAvatarDropdown} />

## ProfileItems

Serves as a wrapper to render "Profile items" section as a unit with inteded common styles

## Basic usage

<Playground>
  <ProfileItems>
    <AlertsDropdown alerts={[notifications[1]]} />
    <InboxDropdown messages={[notifications[0], notifications[2]]} />
    <UserAvatarDropdown user={{}} />
  </ProfileItems>
</Playground>

## Types

```
export type Alert = {};
```

```
export type Message = {};
```
